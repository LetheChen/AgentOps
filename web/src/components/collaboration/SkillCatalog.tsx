/**
 * SkillCatalog — Skill 模板目录（占位）
 *
 * 原型 swimlane-v2.html 中提及「📚 Skill 模板」，但当前页面聚焦在「协作可视化」
 * （左侧栏已显示「当前工作流」），此处保留为后续扩展入口：
 *   - 列出 AgentOps 系统中所有 workflow（从 /api/workflow/list）
 *   - 支持按业务角色/工作流类型筛选
 *   - 点击跳到对应的 run
 */
export function SkillCatalog() {
  return null;
}